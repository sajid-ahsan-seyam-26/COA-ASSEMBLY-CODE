.MODEL SMALL
.STACK 100H
.DATA
.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX

    MOV AH, 2
    MOV DL, 'A'

PRINT_LOOP:
    INT 21H
    INC DL
    CMP DL, '['
    JNE PRINT_LOOP

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN